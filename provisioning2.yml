---
  - hosts: 192.168.88.216
    tasks:
      - name: alterar hostname
        ansible.windows.win_hostname:
          name: CHSERVER
          register: res

      - name: Reboot
        ansible.windows.win_reboot:
          when: res.reboot_required
